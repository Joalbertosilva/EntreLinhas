# Packages

Pacotes compartilhados do monorepo:

- `types/` — tipos TypeScript (enums, interfaces)
- `schemas/` — schemas Zod (validação web + mobile + Edge Functions)
- `config/` — ESLint, Prettier, tsconfig base
